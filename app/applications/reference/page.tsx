// Reference Section — Phase 6.
// TODO: load /data/references.yaml, render via <ReferenceCard>, add tag filtering.
export default function ReferencePage() {
  return <div className="mx-auto max-w-3xl px-6 py-24">Reference — TODO</div>;
}
