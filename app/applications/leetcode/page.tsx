// LeetCode track — Phase 5.
// Per spec: link OUT to existing Blind 75 / NeetCode 150 books. Do not duplicate problems here.
export default function LeetcodePage() {
  return (
    <div className="mx-auto max-w-3xl px-6 py-24">
      {/* TODO: replace with real links to the existing books */}
      LeetCode — see Blind 75 / NeetCode 150 books (links TODO)
    </div>
  );
}
