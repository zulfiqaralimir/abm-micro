// Sidebar reading-progress indicator (chapters read / total).
// TODO (Phase 2): wire to a chapters list + client-side read state (localStorage is fine for v1).
export default function ProgressTracker() {
  return <div className="text-sm text-gray-400">Progress — TODO</div>;
}
